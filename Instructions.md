📋 ИНСТРУКЦИЯ ПО ПОДКЛЮЧЕНИЮ
Схема подключения (упрощенная, без реверса):

                 ARDUINO NANO              

D2 ───────► PWM вход с ресивера

D3 ───────► Светодиод (через резистор 220Ω)

D5 ───────► Левый магнит (через драйвер)

D9 ───────► Правый магнит (через драйвер)

GND ──────► Общая земля

5V/VIN ───► Питание (через стабилизатор)

                            │

               ДРАЙВЕР MX1508 (упрощенно)          

IN1 ─────► D5 (Arduino) - Левый магнит

IN3 ─────► D9 (Arduino) - Правый магнит

GND ─────► Земля Arduino

VCC ─────► +12V (питание)

OUT1 ────► Левый магнит (+)

OUT3 ────► Правый магнит (+)

GND ─────► Магниты (-)

**Подробная схема подключения MX1508:**

          MX1508 Драйвер
         
Arduino D5├──►IN1   OUT1├──► Левый магнит (+)
        
                    OUT2├──► Левый магнит (-) - на GND
         
Arduino D9├──►IN3   OUT3├──► Правый магнит (+)
        
                    OUT4├──► Правый магнит (-) - на GND
      
      GND ├──►GND    VCC├──► +12V питания

**Основные настройки (в коде):**

1. Настройте диапазоны PWM под ваш передатчик:
const int BRAKE_MIN = 1200;   Начало тормозного режима
const int BRAKE_MAX = 1400;  Конец тормозного режима  
const int WORK_MIN = 1500;   Начало рабочего режима
const int NEUTRAL = 1750;    Нейтральное положение

// Измените значения в таблице под свои нужды:
const float VOLTAGES[4][3] = {
  {3.0, 10.0, 10.0}, // Уровень 0
  {3.0, 9.0, 9.0},   // Уровень 1
  {3.0, 8.0, 8.0},   // Уровень 2
  {3.0, 7.0, 7.0}    // Уровень 3
};

// 3. Настройте параметры переключения:
const byte PATTERN_CYCLES = 4;       4 цикла вкл/выкл
const unsigned int PATTERN_TIME = 2000;  За 2 секунды

          Калибровка PWM сигнала:
        Подключите Arduino к компьютеру
        
        Добавьте в setup(): Serial.begin(115200);

static unsigned long lastPrint = 0;
if (millis() - lastPrint > 1000) {
  Serial.print("PWM: ");
  Serial.println(pwm);
  lastPrint = millis();
}


⚙ РАБОЧИЕ РЕЖИМЫ
Тормозной режим (PWM 1200-1400):
- Оба магнита: 6В постоянно
- При отклонении от центра (1300):
  * PWM < 1300: правый магнит пульсирует
  * PWM > 1300: левый магнит пульсирует
- Индикация: соответствует пульсации

Основной режим (PWM 1500+):
Фаза 1 (первые 2 секунды):
- Напряжение растет от минимума до максимума уровня
- Оба магнита пульсируют
- Индикация пульсирует

Фаза 2 (после 2 секунд):
- Постоянное напряжение (из таблицы для уровня)
- При нейтрали: оба магнита постоянно включены
- При отклонении: пульсирует только один магнит
- Индикация соответствует пульсации



Добавьте в loop():
